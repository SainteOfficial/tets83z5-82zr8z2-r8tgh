-- Luna Interface Suite laden
local Luna = loadstring(game:HttpGet("https://raw.githubusercontent.com/Nebula-Softworks/Luna-Interface-Suite/refs/heads/main/source.lua", true))()

-- Fenster erstellen
local Window = Luna:CreateWindow({
    Name = "Einfaches Script Hub", 
    Subtitle = "v1.0",
    LogoID = nil, 
    LoadingEnabled = true,
    LoadingTitle = "Script Hub wird geladen...",
    LoadingSubtitle = "Scripte werden vorbereitet",
    
    ConfigSettings = {
        ConfigFolder = "MeinScriptHub"
    },
    
    KeySystem = false
})

-- Tab 1: Universal erstellen - mit einfachem Icon
local UniversalTab = Window:CreateTab({
    Name = "Universal",
    Icon = "home", -- Einfaches Icon ohne Leerzeichen
    ImageSource = "Material", -- Material als Quelle angeben
    ShowTitle = true
})

-- Paragraph zum Testen hinzufügen
UniversalTab:CreateParagraph({
    Title = "Willkommen", 
    Text = "Dies ist ein einfaches Script Hub mit zwei Tabs"
})

-- Geschwindigkeit & Sprung Sektion
UniversalTab:CreateSection("Geschwindigkeit & Sprung")

-- WalkSpeed Toggle
UniversalTab:CreateToggle({
    Name = "Schneller Laufen",
    Description = "Erhöht deine Laufgeschwindigkeit",
    CurrentValue = false,
    Callback = function(Value)
        if Value then
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 50
        else
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 16
        end
    end
}, "WalkSpeedToggle")

-- JumpPower Toggle
UniversalTab:CreateToggle({
    Name = "Höher Springen",
    Description = "Erhöht deine Sprunghöhe",
    CurrentValue = false,
    Callback = function(Value)
        if Value then
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = 100
        else
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = 50
        end
    end
}, "JumpPowerToggle")

-- Tab 2: Einstellungen
local SettingsTab = Window:CreateTab({
    Name = "Einstellungen",
    Icon = "settings", -- Einfaches Icon ohne Leerzeichen
    ImageSource = "Material", -- Material als Quelle angeben
    ShowTitle = true
})

-- Text zur Bestätigung
SettingsTab:CreateParagraph({
    Title = "Einstellungen Tab", 
    Text = "Hier kannst du verschiedene Einstellungen vornehmen"
})

-- UI-Keybind
local UIBind = SettingsTab:CreateBind({
    Name = "UI ein-/ausblenden",
    Description = "Drücke diese Taste, um das UI ein-/auszublenden",
    CurrentBind = "RightControl",
    HoldToInteract = false,
    Callback = function() end,
    OnChangedCallback = function(Bind)
        Window.Bind = Bind
    end,
}, "UIToggleKeybind")

-- Theming-Sektion hinzufügen
SettingsTab:BuildThemeSection()

-- Konfigurationssektion hinzufügen
SettingsTab:BuildConfigSection()

-- WICHTIG: Automatische Konfiguration laden (muss am Ende sein!)
Luna:LoadAutoloadConfig()
